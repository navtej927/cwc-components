import React from "react";
import { Button } from '@cwcode/button';

interface CardProps {
}

export const Card = (props: CardProps) => {
  return (
    <div>
      CARD
      <Button>CARD JI</Button>
    </div>
  )
}
